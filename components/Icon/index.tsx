import React from "react";

type Props = {
  name: string;
  color?: string;
  style?: any;
  className?: any;
};

const Icon: React.FC<Props> = ({ name, ...props }) => {
  return ();
};

export default Icon;
